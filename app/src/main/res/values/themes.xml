<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ProgettoEmbedded" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_500</item>
        <item name="colorPrimaryVariant">@color/primary_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="toolbarStyle">@style/custom_toolbar</item>
        <item name="android:textViewStyle">@style/custom_textView</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="custom_textView">
        <item name="android:fontFamily">@font/proxima_nova</item>
        <item name="android:textColor">@color/default_text_color</item>
    </style>

    <style name="custom_toolbar" parent="@style/Widget.AppCompat.Toolbar">
        <item name="titleTextColor">@color/white</item>
        <item name="android:fontFamily">@font/proxima_nova</item>
    </style>

</resources>